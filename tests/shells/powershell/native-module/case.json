{
  "id": "powershell-native-module",
  "shell": "powershell",
  "stack": "native-module",
  "title": "a binary module this runtime cannot load, diagnosed rather than loaded silently",
  "supported": true,
  "notes": "The gesture on this editor is a chord the worker configures rather than the line discipline's own character, and this editor reaches its own reader-thread queue only when the reader steps: a fence published between one step and the next is not the one the gesture that follows it is decided against, so this session cannot put the gesture and the fence in a known order. The Windows leg of this package, where the chord and the named-pipe endpoint are the mechanism, is qualified on Windows.",
  "requires": [],
  "order": [
    "all-hosts",
    "user-top",
    "kr-module-refused",
    "kr-module-absent",
    "stack",
    "user-bottom"
  ],
  "binding": "kr-user-binding",
  "checks": [
    "identity",
    "profile_order",
    "profile_once",
    "native_module",
    "user_bindings"
  ],
  "native_module": {
    "name": "KrWrongAbi",
    "marker": "kr-module-refused",
    "note": "What this case ships is a file named like a module whose contents this build's loader refuses, so what it proves is the diagnosis rather than an editor-ABI mismatch: the loader names the module it would not load, the shell's own list of loaded modules does not hold it, and the integration is still qualified afterwards. A module compiled against another ABI of this shell would prove the mismatch itself and needs a compiler and that shell's headers at qualification time, which this corpus does not have."
  },
  "covers": [
    "KR-REQ-07.87",
    "KR-REQ-07.72",
    "KR-REQ-07.73",
    "KR-REQ-07.74"
  ],
  "home": [
    {
      "file": "profile.ps1",
      "path": ".config/powershell/profile.ps1"
    },
    {
      "file": "host-profile.ps1",
      "path": ".config/powershell/Microsoft.PowerShell_profile.ps1"
    },
    {
      "file": "KrWrongAbi.dll",
      "path": "modules/KrWrongAbi/KrWrongAbi.dll"
    }
  ]
}
