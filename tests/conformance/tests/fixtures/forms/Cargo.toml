# A tree the conformance report's own tests read and run. It is a workspace of its own, so the
# repository's workspace never builds it.
[package]
name = "forms"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]
