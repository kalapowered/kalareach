--- a/src/input/binding.rs
+++ b/src/input/binding.rs
@@ -519,6 +519,7 @@
         if self.idx == self.peeked.len() {
             let event = self.event_queue.readch();
             self.peeked.push(event);
+            kr_bridge::note_peeked(self.peeked.len());
         }
         let res = self.peeked[self.idx].clone();
         self.idx += 1;
@@ -566,6 +567,7 @@
             };
             flog!(reader, format!("adding peeked {:?}", newevt));
             self.peeked.push(newevt);
+            kr_bridge::note_peeked(self.peeked.len());
         }
         // Now we have peeked far enough; check the event.
         // If it matches the char, then increment the index.
@@ -638,6 +640,7 @@
         self.event_queue.insert_front(self.peeked.drain(self.idx..));
         self.idx = 0;
         self.subidx = 0;
+        kr_bridge::note_peeked(0);
         std::mem::take(&mut self.peeked)
     }
 
@@ -773,6 +776,7 @@
             "Events left on the queue - missing restart or consume?",
         );
         self.event_queue.insert_front(self.peeked.drain(self.idx..));
+        kr_bridge::note_peeked(0);
     }
 }
 
@@ -785,6 +789,10 @@
 
         // Search for sequence in binding tables.
         loop {
+            // The pending flags say the reader is in the middle of something; this says where the
+            // character itself came from, which is a different question and part of the same
+            // condition.
+            let pushed_back = !self.get_input_data().queue.is_empty();
             let mut evt = self.readch();
             match evt {
                 CharEvent::Readline(ref mut readline_event) => match readline_event.cmd {
@@ -793,6 +801,12 @@
                     | ReadlineCmd::GetKey => {
                         // Typically self-insert is generated by the generic (empty) binding.
                         // However if it is generated by a real sequence, then insert that sequence.
+                        // `get-key` reports the key it reads rather than acting on it, which is
+                        // this reader's equivalent of waiting for a quoted character.
+                        let literal_key = readline_event.cmd == ReadlineCmd::GetKey;
+                        if literal_key {
+                            kr_bridge::note_pending_literal_key(true);
+                        }
                         self.insert_front(std::mem::take(&mut readline_event.key_events));
                         // Issue #1595: ensure we only insert characters, not readline functions. The
                         // common case is that this will be empty.
@@ -806,6 +820,10 @@
                             }
                         });
 
+                        if literal_key {
+                            kr_bridge::note_pending_literal_key(false);
+                        }
+
                         // Hackish: mark the input style.
                         if readline_event.cmd == ReadlineCmd::SelfInsertNotFirst {
                             if let CharEvent::Key(kevt) = &mut res {
@@ -855,6 +873,7 @@
                             kevt.seq.chars().map(u32::from).collect::<Vec<_>>()
                         )
                     );
+                    kr_bridge::note_source_pushed_back(pushed_back);
                     self.push_front(evt);
                     self.binding_execute_matching_or_generic();
                     // One complete key sequence has been resolved and its binding has not run:
@@ -961,6 +980,10 @@
         let mut skipped = std::mem::take(&mut self.get_input_data_mut().event_storage);
         assert!(skipped.is_empty(), "event_storage should be empty");
 
+        // A jump waits for the character it moves to, which is a motion waiting for its target.
+        if arity > 0 {
+            kr_bridge::note_pending_target(true);
+        }
         for _ in 0..arity {
             // Skip and queue up any function codes. See issue #2357.
             let arg: char;
@@ -976,6 +999,9 @@
                 skipped.push(evt);
             }
             self.function_push_arg(arg);
+        }
+        if arity > 0 {
+            kr_bridge::note_pending_target(false);
         }
 
         // Push the function codes back into the input stream.
