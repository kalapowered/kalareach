{
  "note": "A native-proxy table the package ships but the manifest does not declare. It also belongs to another package and expands the package past its declared size.",
  "codes": [
    "connector_plugin_mismatch",
    "connector_undeclared",
    "size_mismatch",
    "undeclared_file"
  ]
}
