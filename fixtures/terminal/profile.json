{
  "capabilities": [
    "cursor-operations",
    "both-screen-buffers",
    "scroll-regions",
    "bracketed-paste",
    "focus-events",
    "sgr-mouse",
    "hyperlinks",
    "synchronised-output",
    "xterm-modifier-keys",
    "kitty-keyboard-subset",
    "true-colour",
    "indexed-palette"
  ],
  "description": "What kr-vt/1 advertises, what it refuses, and the exact identity bytes. Nothing here describes a physical terminal.",
  "device_attributes": {
    "primary": [
      62,
      22
    ],
    "secondary": [
      41,
      1,
      0
    ],
    "tertiary_unit_id": "4B520001"
  },
  "grid_library": {
    "direct_mode_constraints": [
      "A grapheme cluster takes one cell. A ZWJ emoji sequence occupies two cells even though the profile does not advertise mode 2027, and a terminal without it would draw four. A physical profile qualified for direct mode has to cluster the same way.",
      "A wide cell may overhang the right margin. Writing a two-cell character in the last column leaves a row one cell wider than the grid and sets the pending wrap, where xterm blanks the last column and wraps the character. A projected renderer clips or safely replaces the overhanging cell."
    ],
    "notes": [
      "The revision accepts already-parsed actions, so the engine parses once and the library never re-frames a byte. That is what makes a second parse unnecessary rather than merely discouraged.",
      "The width model is configuration, not a fork: generation 9 with narrow ambiguous characters is exactly the pinned kr-vt/1 model.",
      "Grapheme clustering, in-band resize and DECCOLM never reach the library, because the policy layer classifies them before the reducer sees anything. The library's own support for them is therefore not part of the profile.",
      "Raster graphics are disabled in configuration and no image sequence is ever forwarded, so the library's sixel, iTerm2 and Kitty image paths stay unreachable.",
      "The library is built with a writer that accepts no bytes. Every reply comes from the query broker, and the fixtures fail if the library ever writes one.",
      "A sequence the library marks unspecified, at any nesting, is not applied and not forwarded. The mapping is therefore checked by what the library does with it rather than by whether it parsed.",
      "The library clusters the text of one call to its action interface, so the engine holds the final scalar of a text run until it knows what follows. Without that, the same bytes would produce different screens depending on where a read happened to split them."
    ],
    "repository": "https://github.com/wezterm/wezterm",
    "required_patch": [
      {
        "interim": "the snapshot carries None and a reconnecting client re-derives it from the next character it places",
        "reason": "section 8 lists pending wrap among the state a snapshot restores",
        "state": "TerminalState::pending_wrap()"
      },
      {
        "interim": "the snapshot carries None; a restored session behaves as though nothing was saved until the application saves again",
        "reason": "section 8 lists saved cursors among the state a snapshot restores",
        "state": "TerminalState::saved_cursor() as a shared reference with public fields"
      }
    ],
    "revision": "699fd77b44641c43476c945054cfae6518dbd632"
  },
  "identity": "KalaReach(kr-vt/1)",
  "limits": {
    "control_string_bytes": 65536,
    "history_page_bytes": 1048576,
    "history_page_rows": 1000,
    "max_cells": 262144,
    "max_cols": 2048,
    "max_rows": 1024,
    "osc52_string_bytes": 1048576,
    "passthrough_depth": 4,
    "response_lane_queue_bytes": 131072,
    "responses_per_second": 256,
    "row_cache_bytes": 8388608,
    "session_bytes": 67108864
  },
  "name": "profile",
  "profile": "kr-vt/1",
  "term": "xterm-256color",
  "tracked_ansi_modes": [
    4,
    20
  ],
  "tracked_dec_modes": [
    1,
    5,
    6,
    7,
    8,
    12,
    25,
    45,
    66,
    67,
    69,
    1000,
    1002,
    1003,
    1004,
    1005,
    1006,
    1007,
    1034,
    1047,
    1048,
    1049,
    2004,
    2026
  ],
  "withheld": [
    {
      "feature": "sixel-graphics",
      "reason": "terminal raster graphics are disabled; images travel through the file protocol"
    },
    {
      "feature": "kitty-graphics",
      "reason": "terminal raster graphics are disabled; images travel through the file protocol"
    },
    {
      "feature": "iterm2-inline-images",
      "reason": "terminal raster graphics are disabled; images travel through the file protocol"
    },
    {
      "feature": "grapheme-clustering-mode-2027",
      "reason": "a physical terminal on the other side may cluster differently"
    },
    {
      "feature": "in-band-resize-mode-2048",
      "reason": "resize travels over the PTY or ConPTY notification, not a second in-band channel"
    },
    {
      "feature": "deccolm-mode-3",
      "reason": "rows and columns belong to the geometry owner"
    },
    {
      "feature": "selective-erase",
      "reason": "DECSCA is not implemented, so DA1 does not claim it"
    },
    {
      "feature": "132-column-mode",
      "reason": "DECCOLM belongs to the geometry owner, so DA1 does not claim it"
    },
    {
      "feature": "title-reporting",
      "reason": "reporting the title back into the stream lets output become input"
    },
    {
      "feature": "answerback",
      "reason": "kr-vt/1 has no answerback string, so ENQ is answered with silence"
    }
  ]
}
